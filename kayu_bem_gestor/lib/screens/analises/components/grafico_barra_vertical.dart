import 'package:flutter/material.dart';

class GraficoBarraVertical extends StatelessWidget {
  const GraficoBarraVertical({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(child: Container(color: Colors.blue, height: 150));
  }
}
